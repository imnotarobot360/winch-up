node_modules/
.next/
out/
build/
dist/

.env
.env.local
.env.*.local

.vercel
*.tsbuildinfo
next-env.d.ts

supabase/.branches
supabase/.temp
supabase/.env

# Generated VAPID pair. `npm run push:keys` writes here so the private key lands in a file
# rather than a terminal that gets scrolled back through or pasted into a chat. Copy the three
# values into Vercel and delete it.
vapid-keys.txt

.DS_Store
Thumbs.db
*.log
npm-debug.log*
.idea/
.vscode/*
!.vscode/extensions.json

# Playwright
/test-results/
/playwright-report/
/blob-report/

.vercel
.env*
